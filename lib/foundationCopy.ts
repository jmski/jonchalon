// Page-specific copy that doesn't need CMS editorial workflow.
// The kinetic line is the page's single argument; it lives in the codebase
// alongside its rendering, sourced from canonical-copy.md.
// Italic anchor uses {{double-braces}} per the renderHeadline convention.

export const foundationKineticLine = 'Eight weeks. One week at a {{time}}.'
